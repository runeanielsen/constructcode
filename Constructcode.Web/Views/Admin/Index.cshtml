@model IEnumerable<Constructcode.Web.ViewModels.PostViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "_AdminLayout";
}

<div class="col-lg-12">
    @foreach (var post in Model)
    {
        <div>
            <a asp-controller="Post" asp-action="EditPost" asp-route-id="@post.Id">
                <h3>@post.Title</h3>
            </a>
            <div>
                @Html.Raw(post.Introduction)
            </div>
        </div>
    }
</div>